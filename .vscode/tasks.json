{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Memcached",
            "type": "shell",
            "command": "./configure && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}